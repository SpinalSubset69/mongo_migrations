const config = require("../config");

//This method will look for the id in all the databases
//must send an object with the filter
async function lookUserOnDbs(filter) {

}






/* let db_to_connect;
  switch (db) {
    case "becal":
      db_to_connect = config.sistema_becal_url;
      break;
    case "uso":
      db_to_connect = config.db_en_uso_url;
      break;
    case "db1":
      db_to_connect = config.db1;
      break;
    case "db2":
      db_to_connect = config.db2;
      break;
    default:
      db_to_connect = config.objective_db;
  } */